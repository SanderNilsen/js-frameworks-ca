import styled from "styled-components";

const Card = styled.div`
  background: #fff;
  border-radius: 16px;
  padding: 1rem;
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
  display: grid;
  gap: 1rem;
`;

export default Card;